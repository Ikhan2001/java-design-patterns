/*
package design.pattern.behavioral.observerpattern.subject;

import design.pattern.behavioral.observerpattern.observer.Observer;

public interface Subject {
    void subscribe(Observer observer);
    void unsubscribe(Observer observer);
    void notifyObservers(String message);
}
*/
//This is also a way to make subject or we want similar
// functionality for multiple class we can go for abstract class with similar functionality
